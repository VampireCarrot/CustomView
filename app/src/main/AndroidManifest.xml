<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lwd.customview">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".basic.Clock.ClockActivity" />
        <activity android:name=".basic.paint.PaintExerciseActivity" />
        <activity android:name=".basic.path.PathEcerciseActivity" />
        <activity android:name=".basic.shader.ShaderExercixeActivity" />
        <activity android:name=".basic.animpath.PathAnimActivity" />
        <activity android:name=".basic.bezierPath.BezierActivity" />
        <activity android:name=".basic.GameOne.GameOneActivity" />
        <activity android:name=".basic.GameTwo.TetrisActivity" />
        <activity android:name=".basic.CurtainView.CurtainActivity"></activity>
    </application>

</manifest>